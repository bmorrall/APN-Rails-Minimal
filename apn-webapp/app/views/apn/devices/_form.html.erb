<%= form_for(@apn_device) do |f| %>
  <% if @apn_device.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@apn_device.errors.count, "error") %> prohibited this apn_device from being saved:</h2>

      <ul>
      <% @apn_device.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="clearfix">
    <%= f.label :token %>
    <div class="input">
      <%= f.text_field :token, :class => "xxlarge" %>
    </div>
  </div>
  <div class="actions">
    <%= f.submit class: "btn primary" %>
    <% unless @apn_device.new_record? %><%= link_to 'Show', @apn_device, class: "btn info" %><% end %>
    <%= link_to 'Back', apn_devices_path %>
  </div>
<% end %>
